#!/usr/bin/env node
// import { program } from "commander";
console.log(121);

function xx(s: string) {
  return "s" + s;
}

export function xx1(xx: string) {
  return xx + "123";
}

export default xx;
